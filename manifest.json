{
  "name": "mailer-postmark",
  "author": "Alan Plum",
  "version": "0.1",
  "description": "Mailer task for Foxx using Postmark.",

  "exports": {
    "mailer": "mailer.js"
  },

  "defaultDocument": "",

  "lib": ".",

  "configuration": {
    "apiKey": {
      "description": "Your Postmark server's API key.",
      "type": "string"
    },
    "jobType": {
      "description": "Name of the job type to register.",
      "type": "string",
      "default": "mailer.postmark"
    },
    "maxFailures": {
      "description": "Maximum number of times to retry a job if it fails.",
      "type": "integer",
      "default": 0
    }
  }
}
